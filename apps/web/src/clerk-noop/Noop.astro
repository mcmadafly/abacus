---
// No-op stand-in for Clerk UI components in the no-auth ("waitlist") production
// build, where the Clerk integration is omitted. Auth routes redirect home
// before any of these would render, so this just keeps imports resolvable.
---

<slot />
